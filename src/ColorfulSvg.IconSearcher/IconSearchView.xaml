<UserControl x:Class="ColorfulSvg.IconSearcher.IconSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d">
    <Grid x:Name="MainContentArea" Margin="0,12,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*" />
            <ColumnDefinition x:Name="WorkspaceSpacerColumn" Width="10" />
            <ColumnDefinition x:Name="WorkspaceColumn" Width="400" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="6" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="6" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="SearchQueryTextBox" Padding="10,6" MinHeight="34" KeyDown="SearchQueryTextBox_KeyDown" />
                    <Button x:Name="SearchButton" Grid.Column="2" Padding="10,6" MinHeight="34" Click="SearchButton_Click">
                        <TextBlock Text="打开搜索" />
                    </Button>
                    <Button x:Name="BrowserBackButton" Grid.Column="4" Padding="10,6" MinHeight="34" Click="BrowserBackButton_Click">
                        <TextBlock Text="后退" />
                    </Button>
                    <Button x:Name="BrowserForwardButton" Grid.Column="6" Padding="10,6" MinHeight="34" Click="BrowserForwardButton_Click">
                        <TextBlock Text="前进" />
                    </Button>
                    <Button x:Name="BrowserRefreshButton" Grid.Column="8" Padding="10,6" MinHeight="34" Click="BrowserRefreshButton_Click">
                        <TextBlock Text="刷新" />
                    </Button>
                    <Button x:Name="WorkspaceToggleButton" Grid.Column="10" Padding="10,6" MinHeight="34" Click="WorkspaceToggleButton_Click">
                        <TextBlock Text="隐藏工作区" />
                    </Button>
                </Grid>

                <Border Grid.Row="2" Padding="8" Background="#F8FAFC" BorderBrush="#E6EBF4" BorderThickness="1" CornerRadius="22" MinHeight="720">
                    <wv2:WebView2 x:Name="BrowserView" PreviewMouseLeftButtonDown="BrowserView_PreviewMouseLeftButtonDown" />
                </Border>
            </Grid>
        </Border>

        <Border Grid.Column="0" Margin="24,0,0,24" Padding="12" Width="340" HorizontalAlignment="Left" VerticalAlignment="Bottom" Background="#F7FBFF" BorderBrush="#DCE4EF" BorderThickness="1" CornerRadius="16" Panel.ZIndex="2">
            <StackPanel>
                <TextBlock FontWeight="SemiBold" Foreground="{Binding StatusBrush}" Text="{Binding StatusTitle}" />
                <TextBlock Margin="0,8,0,0" Foreground="{StaticResource TextSecondaryBrush}" Text="{Binding StatusMessage}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Button x:Name="WorkspacePeekButton" Grid.Column="0" Width="104" Height="40" Margin="0,0,14,14" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="WorkspacePeekButton_Click" Visibility="Collapsed" Panel.ZIndex="3">
            <TextBlock Text="打开工作区" />
        </Button>

        <GridSplitter x:Name="WorkspaceSplitter" Grid.Column="1" Width="10" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#20DCE4EF" Cursor="SizeWE" ResizeDirection="Columns" ResizeBehavior="PreviousAndNext" ShowsPreview="True" Panel.ZIndex="4" />

        <Border x:Name="WorkspacePanel" Grid.Column="2" MinWidth="160" Background="Transparent" Visibility="Visible">
            <Border Background="Transparent" CornerRadius="24" Padding="0">
                <Border Background="#F5F8FD" BorderBrush="#DCE4EF" BorderThickness="1" CornerRadius="20">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" CanContentScroll="False">
                        <Grid Margin="18,16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="24" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Border Style="{StaticResource CardStyle}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="16" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="16" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="16" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="16" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="16" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="16" />
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="18" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <StackPanel>
                                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="导出工作区" />
                                            <TextBlock Margin="0,8,0,0" Style="{StaticResource BodyTextStyle}" Text="{Binding SelectionSummary}" />
                                        </StackPanel>

                                        <StackPanel Grid.Column="1">
                                            <Button x:Name="ClearSelectedIconsButton" Padding="10,6" IsEnabled="{Binding CanClearSelectedItems}" Click="ClearSelectedIconsButton_Click">
                                                <TextBlock Text="清空已选" />
                                            </Button>
                                            <Button x:Name="CollapseWorkspaceButton" Margin="0,6,0,0" Padding="10,6" Click="CollapseWorkspaceButton_Click">
                                                <TextBlock Text="收起" />
                                            </Button>
                                        </StackPanel>
                                    </Grid>

                                    <Border Grid.Row="2" Padding="12" Background="#FBFCFE" BorderBrush="#E6EBF4" BorderThickness="1" CornerRadius="14">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="10" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <StackPanel>
                                                <TextBlock FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Text="导入来源" />
                                                <TextBlock Margin="0,6,0,0" Foreground="{StaticResource TextSecondaryBrush}" Text="{Binding BrowserHint}" TextWrapping="Wrap" />
                                            </StackPanel>

                                            <StackPanel Grid.Column="2">
                                                <Button x:Name="ImportCurrentIconButton" Padding="10,6" IsEnabled="{Binding CanImportCurrentIcon}" Click="ImportCurrentIconButton_Click">
                                                    <TextBlock Text="添加当前图标" />
                                                </Button>
                                                <Button x:Name="ImportVisibleIconsButton" Margin="0,6,0,0" Padding="10,6" IsEnabled="{Binding CanSelectAllCurrentPage}" Click="ImportVisibleIconsButton_Click">
                                                    <TextBlock Text="全选当前页" />
                                                </Button>
                                                <Button x:Name="ImportLocalSvgFilesButton" Margin="0,6,0,0" Padding="10,6" IsEnabled="{Binding CanImportLocalSvgFiles}" Click="ImportLocalSvgFilesButton_Click">
                                                    <TextBlock Text="导入 SVG 文件" />
                                                </Button>
                                            </StackPanel>
                                        </Grid>
                                    </Border>

                                    <Border Grid.Row="4" Padding="12" Background="#FBFCFE" BorderBrush="#E6EBF4" BorderThickness="1" CornerRadius="14">
                                        <StackPanel>
                                            <TextBlock FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Text="统一资源键前缀" />
                                            <TextBlock Margin="0,6,0,0" Foreground="{StaticResource TextSecondaryBrush}" Text="例如输入 icons/ 或 assets/svg/，点击后会批量更新右侧列表中的资源键前缀。" TextWrapping="Wrap" />
                                            <Grid Margin="0,10,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <TextBox x:Name="ResourceKeyPrefixTextBox"
                                                         Padding="10,6"
                                                         MinHeight="36"
                                                         Text="{Binding ResourceKeyPrefix, UpdateSourceTrigger=PropertyChanged}" />
                                                <Button x:Name="ApplyResourceKeyPrefixButton"
                                                        Grid.Column="2"
                                                        Padding="10,6"
                                                        Click="ApplyResourceKeyPrefixButton_Click">
                                                    <TextBlock Text="应用前缀" />
                                                </Button>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <TextBlock Grid.Row="6" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Text="导出方式" />
                                    <ComboBox x:Name="ExportModeComboBox" Grid.Row="8" MinHeight="36" Padding="10,6" DisplayMemberPath="Label" ItemsSource="{Binding ExportModes}" SelectedItem="{Binding SelectedExportModeOption}" />
                                    <TextBlock Grid.Row="10" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Text="输出文件" />

                                    <Grid Grid.Row="12">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="14" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="18" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="8" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <TextBox x:Name="OutputPathTextBox" Padding="10,6" MinHeight="36" Text="{Binding OutputPath, UpdateSourceTrigger=PropertyChanged}" />
                                            <Button x:Name="BrowseOutputPathButton" Grid.Column="2" Padding="10,6" Click="BrowseOutputPathButton_Click">
                                                <TextBlock Text="选择路径" />
                                            </Button>
                                        </Grid>

                                        <Border Grid.Row="2" Padding="10" Background="{Binding StatusBackground}" CornerRadius="14">
                                            <StackPanel>
                                                <TextBlock FontWeight="SemiBold" Foreground="{Binding StatusBrush}" Text="{Binding StatusTitle}" />
                                                <TextBlock Margin="0,4,0,0" Foreground="{StaticResource TextSecondaryBrush}" Text="{Binding StatusMessage}" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </Border>

                                        <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto">
                                            <ItemsControl ItemsSource="{Binding SelectedItems}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Margin="0,0,0,8" Padding="10" Background="#FBFCFE" BorderBrush="#E6EBF4" BorderThickness="1" CornerRadius="14">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto" />
                                                                    <ColumnDefinition Width="10" />
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="10" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>

                                                                <Border Width="40" Height="40" Background="{StaticResource SurfaceAltBrush}" CornerRadius="10">
                                                                    <Image Width="22" Height="22" HorizontalAlignment="Center" VerticalAlignment="Center" Source="{Binding PreviewImage}" />
                                                                </Border>

                                                                <StackPanel Grid.Column="2">
                                                                    <TextBlock FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Text="{Binding Id}" />
                                                                    <TextBox Margin="0,6,0,0" Padding="10,6" MinHeight="34" Text="{Binding ResourceKey, UpdateSourceTrigger=PropertyChanged}" />
                                                                </StackPanel>

                                                                <Button Grid.Column="4" VerticalAlignment="Top" Padding="8,6" Click="RemoveSelectedIconButton_Click" CommandParameter="{Binding}">
                                                                    <TextBlock Text="删除" />
                                                                </Button>
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Grid>

                                    <Grid Grid.Row="14">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="8" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <Button x:Name="ExportSvgZipButton" Padding="10,6" MinHeight="36" IsEnabled="{Binding CanExport}" Click="ExportSvgZipButton_Click">
                                            <TextBlock Text="下载 SVG ZIP" />
                                        </Button>
                                        <Button x:Name="ExportButton" Grid.Column="2" Padding="10,6" MinHeight="36" IsEnabled="{Binding CanExport}" Click="ExportButton_Click">
                                            <TextBlock Text="{Binding ExportButtonLabel}" />
                                        </Button>
                                    </Grid>
                                </Grid>
                            </Border>

                            <Border Grid.Row="2" Style="{StaticResource CardStyle}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="18" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <StackPanel>
                                        <TextBlock Style="{StaticResource SectionTitleStyle}" Text="当前预览" />
                                        <TextBlock Margin="0,8,0,0" Style="{StaticResource BodyTextStyle}" Text="这里显示当前 yesicon 页面图标或本地导入 SVG 的 DrawingImage 预览和来源信息。确认合适后，再把它保留在右侧导出列表。" />
                                    </StackPanel>

                                    <Grid Grid.Row="2">
                                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" Foreground="{StaticResource TextMutedBrush}" Text="在左侧打开一个 yesicon 图标详情页，或在右侧导入本地 SVG 以查看预览">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasActiveResult}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                        <ContentControl Content="{Binding ActiveResult}">
                                            <ContentControl.ContentTemplate>
                                                <DataTemplate>
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="18" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="18" />
                                                            <RowDefinition Height="*" />
                                                        </Grid.RowDefinitions>

                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>

                                                            <StackPanel>
                                                                <TextBlock FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Text="{Binding Id}" />
                                                                <TextBlock Margin="0,8,0,0" Foreground="{StaticResource TextSecondaryBrush}" Text="{Binding CollectionCaption}" />
                                                            </StackPanel>

                                                            <Border Grid.Column="1" Padding="12,6" Background="{StaticResource AccentSoftBrush}" CornerRadius="999">
                                                                <TextBlock FontSize="12.5" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}" Text="{Binding ResourceKey}" />
                                                            </Border>
                                                        </Grid>

                                                        <Border Grid.Row="2" Padding="14" CornerRadius="16">
                                                            <Border.Background>
                                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                    <GradientStop Color="#F8FBFF" Offset="0" />
                                                                    <GradientStop Color="#EEF4FF" Offset="0.6" />
                                                                    <GradientStop Color="#E7F8F1" Offset="1" />
                                                                </LinearGradientBrush>
                                                            </Border.Background>
                                                            <Grid>
                                                                <Border Width="190" Height="190" HorizontalAlignment="Center" VerticalAlignment="Center" Background="#F6FFFFFF" BorderBrush="#CCFFFFFF" BorderThickness="1" CornerRadius="16">
                                                                    <Grid>
                                                                        <Image Width="132" Height="132" HorizontalAlignment="Center" VerticalAlignment="Center" Source="{Binding PreviewImage}" />
                                                                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource TextMutedBrush}" Text="Loading">
                                                                            <TextBlock.Style>
                                                                                <Style TargetType="TextBlock">
                                                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                                                    <Style.Triggers>
                                                                                        <DataTrigger Binding="{Binding PreviewImage}" Value="{x:Null}">
                                                                                            <Setter Property="Visibility" Value="Visible" />
                                                                                        </DataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </TextBlock.Style>
                                                                        </TextBlock>
                                                                    </Grid>
                                                                </Border>
                                                            </Grid>
                                                        </Border>

                                                        <StackPanel Grid.Row="4">
                                                            <Border Padding="14" Background="#FBFCFE" BorderBrush="#E6EBF4" BorderThickness="1" CornerRadius="14">
                                                                <StackPanel>
                                                                    <TextBlock Foreground="{StaticResource TextSecondaryBrush}" Text="{Binding DetailStateLabel}" TextWrapping="Wrap" />
                                                                    <TextBlock Margin="0,8,0,0" Foreground="{StaticResource TextSecondaryBrush}" Text="{Binding AuthorLabel}" TextWrapping="Wrap" />
                                                                    <TextBlock Margin="0,6,0,0" Foreground="{StaticResource TextSecondaryBrush}" Text="{Binding LicenseLabel}" TextWrapping="Wrap" />
                                                                    <TextBlock Margin="0,6,0,0" Foreground="{StaticResource TextSecondaryBrush}" Text="{Binding KeywordLabel}" TextWrapping="Wrap" />
                                                                </StackPanel>
                                                            </Border>
                                                        </StackPanel>
                                                    </Grid>
                                                </DataTemplate>
                                            </ContentControl.ContentTemplate>
                                        </ContentControl>
                                    </Grid>
                                </Grid>
                            </Border>
                        </Grid>
                    </ScrollViewer>
                </Border>
            </Border>
        </Border>
    </Grid>
</UserControl>
